<resources>
    <!-- the theme applied to the application or activity -->
    <style name="AppTheme"
        parent="@android:Theme.Holo">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionMenuTextColor">@style/MyActionBarTitleText</item>
        <item name="android:windowBackground">@color/GrayDarker</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@android:Widget.Holo.Light.ActionBar.Solid.Inverse">
        <!--<item name="android:displayOptions">showHome</item>-->
        <item name="android:background">@color/win8_teal</item>
        <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
    </style>

    <!-- ActionBar title text -->
    <style name="MyActionBarTitleText"
        parent="@android:TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">#fff</item>
    </style>

</resources>
